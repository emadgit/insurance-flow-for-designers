export const emailRegex = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i;
// We can have a config object to set a minimum/maximum required age to use the product
export const config = {
    MIN_AGE: 16,
    MAX_AGE: 80
}